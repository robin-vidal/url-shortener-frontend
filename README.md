# url-shortener-frontend

React frontend for the URL Shortener project, built with Vite.

## Setup

```bash
npm install
cp .env.example .env
```

## Run

```bash
npm run dev
```

## SonarCloud

Organization: `robin-vidal`
Project key: `robin-vidal_url-shortener-backend`

Required GitHub secret: `SONAR_TOKEN`.
